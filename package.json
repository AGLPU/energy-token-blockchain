{
  "name": "energy-token-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for Rooftop Solar Energy Marketplace",
  "scripts": {
    "compile": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && hardhat compile",
    "test": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && hardhat test",
    "node": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && hardhat node",
    "deploy:local": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "chai": "^4.3.0",
    "ethers": "^6.4.0",
    "fs-extra": "^11.2.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
